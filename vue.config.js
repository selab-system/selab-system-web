const { defineConfig } = require('@vue/cli-service')
module.exports = defineConfig({
    transpileDependencies: true,
    lintOnSave: false,
    // devServer: {
    // 端口号
    // port: "",
    // 是否启用https
    // https: false,
    // 自动打开浏览器
    // open: true,
    // 配置代理
    // proxy: {
    //   "/api": {
    //     target: "url",
    //     ws: true,

    //   }
    // }


})

